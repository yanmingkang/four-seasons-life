# 第8、31格影片局部修补

日期：2026-09-13。用户明确允许本地逐帧修补，并确认有权移除第8格水印。本轮只处理这两段影片及其接入，不修改骰子、难度、结局称号、三选项或玩家存档。

## 修补结果

- **第8格**：右下“可灵 AI”区域位于空白背景。仅对原生1280×720画面中的 `(1138,662,128,44)` 小区域逐帧使用周边像素插值；未裁切或放大整个镜头。原有AIGC元数据字符串保留，不将去水印解释为纯人工制作。
- **第31格**：每帧拟合纸张四边，通过纸面透视清理错误红字并写入“协商方案”。保护与纸边相连的手指，保留纸边、原正文灰线与桌面道具。标题不表明已经签署或同意方案，不替玩家预先作选择。
- **非破坏性接入**：仅将这两项视频及封面的清单路径改为 `public/cinematics/team-20260912-repaired-20260913/`。桌面原片、原集成8段视频和8张封面均保留且哈希未变化，其他6项路径不变，第13格仍不配视频。
- 两张封面均从修补后的视频0.1秒处提取，不用一张静态修正版冒充整片修复。

## 媒体保真与限制

| 检查 | 第8格 | 第31格 |
| --- | --- | --- |
| 视频 | 1280×720，24 fps，121帧 | 1280×720，24 fps，121帧 |
| 视频帧时长 | 5.041667秒 | 5.041667秒 |
| 容器时长 | 5.041667秒 | 5.050秒，与桌面原片相同 |
| 原声音 | 原片无音轨，仍保持静音 | 原AAC音频包直接复制，音频哈希相同 |
| 帧时间戳 | 121帧逐项相同 | 121帧逐项相同 |
| 编码与加载 | H.264/yuv420p，faststart | H.264/yuv420p，faststart |
| 全片解码 | 通过 | 音视频均通过 |
| 当前文件大小 | 1,564,679字节 | 1,530,705字节 |

第31格旧兼容转码容器为5.056秒，新文件回到原片5.050秒：不是剪掉片尾，121帧和原声音全部保留。清单原先的5056毫秒提示保持，播放器仍等待原生 `ended`，不按提示时间截片。

修补不是无损处理：第8格局部噪点由插值替代，极度放大会更平滑；第31格旧字所占的纸面像素按实际纸面光照重建。整片H.264重新编码会有细微压缩差异。“修补区外像素相同”仅指最终编码前，不声称最终MP4全画面逐像素相同。

## 验收

- 逐帧画面检查：第8格检查121帧水印区域及原生关键帧；第31格检查覆盖121帧的纸面序列、前后对照和5张原生关键帧，另有独立审查。未见明显水印残留、矩形补丁、旧红字、手指被覆盖或文字脱离纸面。逐帧静态审查不等同于所有设备上的主观流畅度保证。
- 第31格自动记录：121帧的编码前改动全部位于限定纸面区域，纸边跟踪回退0帧。
- 全量单测 **507项：506通过、1项原有可选Chrome测试跳过、0失败**。包括现有媒体解码、帧数、时长、动态画面和封面回归。
- 当前生产构建浏览器专项 **4/4通过**：第8、31格分别在1280×800、844×390下，通过真实UI掷骰、行走、加载封面、播放到原生片尾，再展示完整三个选项。无静态备用、提前出题、页面或WebGL错误。
- 两片GET/HEAD/Range及封面共10项HTTP检查通过，`public`、`dist`、HTTP响应的实际媒体哈希一致。Range沿用完整200响应，并非新增206分段下载。
- 试玩网关补齐正式清单视频/封面的**精确白名单**，仍拒绝目录中任意文件、说明文档及无鉴权访问，不扩大为整个目录公开。
- 用户本地4173：主页、新清单、两段修补MP4均返回200，响应媒体哈希与本地新片相同。原服务未被停止或重启。

生产主包 `main-RcJpmlR2.js`，样式 `main-CFWGk2CE.css`。浏览器专项使用独立服务器和隔离存储，前置历史来自合法引擎回放，不冒称本轮完成4局或20局完整游戏；真实AI/API调用0，未上传GitHub或发布公网。

## 复现与证据

- 第8格处理：`node tools/repair-cell08-cinematic.mjs`。
- 第31格处理：使用带numpy、Pillow的本机Python运行 `tools/repair-cell31-cinematic.py --publish`；需要工具内列出的本机ffmpeg/ffprobe和微软雅黑粗体字体。字体不随项目分发。
- 两个脚本限定输入与新版本目标，已有输出及报告一致时仅校验后跳过，陌生或变更输出拒绝覆盖。失败的中间产物仅留在非公开 `test-results/`，不接入游戏。
- [第8格媒体与完整检查](../test-results/cinematic-repair/cell08-report.json)、[视觉检查边界](../test-results/cinematic-repair/cell08-visual-review.md)
- [第31格媒体、声音与121帧追踪](../test-results/cinematic-repair/cell31-report.json)
- [单测完整日志](../test-results/cinematic-repair/unit-suite.txt)
- [4项生产浏览器验收](../test-results/repaired-cinematics/browser-production.json)
- [第31格游戏内播放](../test-results/repaired-cinematics/cell-31-1280x800-playing.png)
- [第8格小横屏三选卡](../test-results/repaired-cinematics/cell-08-844x390-three-choices.png)
