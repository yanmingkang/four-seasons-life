# 场景人物接入与完整试玩验收

项目：`D:\知乎黑客松\four-seasons-life`。本轮范围为恢复本地启动、接入已有生活场景模块，以及实际浏览器回归；不涉及公开部署或真实模型额度验收。

## 已完成的接入

- `addReferenceScenePopulation()` 已接进 `createCinematicScene3D()`，在环境静态合批前创建人物与物件。场景预览和过场制作共用这一入口。
- 40 个场景均有对应生活内容，共新增 57 位场景人物。16 个安静场景不额外加人，改以杯子、衣物、书籍、行李等生活痕迹丰富画面；原有刘看山、刘看雨及其他角色仍保留。
- 修正坐姿高度、朝向、人物重叠，以及第 4 场景物件越出会议桌、第 28 场景文件缺少承托桌面等问题。
- 新物件参与合批与资源释放；反复打开/关闭场景时清除预览状态，避免使用上一次场景的加载标记。
- 保留实际 3D 地图和 42 个建筑，“建筑外观”选项保持移除，现有游戏规则、存档和 API 配置没有因本轮修改而替换。
- 八段本地短片已同步重新渲染，每段 1280×720、24 fps、96 帧、4 秒。这些是程序化 Three.js 视频，不是 AI 视频模型生成，也不是团队美术交付的视频。

## 当前验收记录

最终构建时间：2026-09-11 21:19:47（北京时间）。

- 入口：`index-bLbxfrir.js`
- 主程序：`main-C4tj2a_f.js`
- 场景预览：`scene-preview-DaJ1XGgt.js`
- `dist/index.html` SHA-256：`7474439f9925ce36d49927fc25fd6afdfaf9fef02fadd46d6906a5cf9ce08648`

| 验收 | 结果与边界 |
| --- | --- |
| 单元测试 | 350 项：349 通过，0 失败，1 跳过；未启用的公开分享浏览器测试不算通过 |
| 实际完整旅程 | 通过。从空存档经 UI 开局，7 次实际选择，春夏秋冬到通关；首轮真实 3D 掷骰和连续行走 |
| 刷新与存档 | 第 18 站刷新恢复，22 次存档写入均可重放、历史不倒退 |
| 结局与下载 | 四季总结、10 项关键回顾、文字手记及 900×3040 PNG 实际下载通过；未经历的事件不伪造成玩家记录 |
| 八段视频 | 全部解码、播放、自然结束、暂停/恢复、跳过、取消、失效备用、横屏与键盘操作通过 |
| 全部场景与横竖屏 | 40／40 逐项通过，全部为真实 WebGL 且生活模块已接入；新增人数逐格匹配、42 个建筑保留、真实拖拽旋转、返回地图/恢复选项、横屏及竖屏暂停/恢复通过，浏览器错误 0 |
| 生活系统与提前结束 | 通过。8 个背包入口、导师与伙伴、刷新保持、自动关东煮、情绪耗尽结局、结局分享、继承开关及 schema1/旧存档兼容均通过；提前结束等分支使用可重放预置状态，不冒称逐格实际游玩 |
| 用户实际 4173 页面 | 21:32 桌面 1440×900 和横屏 844×390 均通过：第 4 场景生活模块已加载、新增 7 人、42 个建筑保留，仅有“走进场景 / 地图位置”按钮，返回地图和存档保持正常，错误与真实 API 调用均为 0 |

完整旅程为了可重现，在测试浏览器中固定骰子为 6，落点为 6、12、18、24、30、36、40；生产骰子的随机规则没有修改。40 个场景的逐项检查是另一套独立验收，不将这 7 次落点冒称为 40 次实际选择。

上述两套完整验收均对应同一个最终构建哈希。之前仅记录到第 12 个场景的结果不足以诊断第 13 格；本次已实际检查到第 40 格并完成后续交互检查，最终报告为 `passed: true`。

## 证据与复测

- 全场景报告：`test-results/reference-integration/report.json`。记录每一格、加载人数、构建哈希和失败位置；关键截图模式仍执行全部 40 格断言，报告内列出本次实际生成的截图。
- 完整旅程：`test-results/full-playthrough/report.json`，以及同目录的 `report-build-7474439f9925.json`、`journey-share.png`。
- 生活系统：`test-results/life-expansion/acceptance.json`。
- 用户实际入口：`test-results/scene-controls/report.json`。4173 运行 Vite 源码服务，生产构建哈希另由两套隔离生产验收记录；两者不混淆。
- 视频验收：`test-results/cinematic-video-validation.json`；制作时间与文件参数：`test-results/cinematic-production/render-metadata.json`。

复测命令：`npm test`、`npm run build`、`node tests/reference-integration-browser.mjs`、`node tests/journey-complete-browser.mjs`、`node tests/life-ui-browser.mjs`、`node tests/cinematics-browser.mjs`。场景验收可设 `REFERENCE_SCREENSHOTS=key`；完整旅程可设 `FULL_PLAYTHROUGH_SCREENSHOTS=key`，只减少截图，不减少交互断言。

## 本地入口与范围限制

本地游戏入口为 `http://127.0.0.1:4173/`。启动器 `启动游戏.cmd` / `start-game.ps1` 会复用正常运行的游戏服务；本轮没有停止用户原有 4173 服务。

验收使用独立浏览器存储和隔离测试端口，业务 API 被拦截为模拟或备用响应，没有覆盖用户存档，没有调用真实模型或消耗知乎接口额度。因此这些结果不证明真实 AI 额度有效，也不证明外网试玩链接可用。本轮未提交或推送 GitHub，未公开发布网站。
