# 本版参考、取舍与实现记录

本次目标是把四时人生做成有连续空间感的 3D 小镇旅程，同时保留骰子决定落点、玩家决定生活选择、知乎经验用于复盘的核心循环。本文件说明参考材料怎样影响了当前实现，不作为已经达到某种行业最佳效果的结论。

## 空间与植被

用户提供的视频用于确定第三人称跟随、连贯道路、低多边形城镇和路边细节的方向。实现时采用项目自己的道路曲线、地形、镜头和几何角色，没有复制视频的贴图、文字或人物。

查看了 [Bruno Simon 的 folio-2025](https://github.com/brunosimon/folio-2025) 中 `Foliage.js` 的实例叶片及遮挡处理思路。本项目在 `src/scenery.js` 中使用带透明裁切的叶片实例形成树冠，并保留多种模型树木、灌木、花草和岩石，让近景轮廓更丰富。镜头前的遮挡物依据角色与镜头的位置调整显示，减少跟随时看不见角色的问题。

这里学习的是用实例减少重复物件的绘制成本、让前景为角色让出视线的方法。具体几何、排列、纹理和实现代码由本项目制作，没有把 Bruno 的完整场景或源文件复制进来。该参考项目的许可见其 [MIT 文件](https://github.com/brunosimon/folio-2025/blob/main/license.md)。

城市细节实际使用 [KayKit City Builder Bits](https://github.com/KayKit-Game-Assets/KayKit-City-Builder-Bits-1.0) 的建筑、车辆、座椅和街道物件；自然细节使用 [Kenney Nature Kit](https://kenney.nl/assets/nature-kit) 的树、花、灌木、岩石等。两套本地素材均附原始 CC0 文件。当前载入清单是城市 12 个、自然 11 个，备用文件及出处列于 `THIRD_PARTY_NOTICES.md`。

## 动作与阅读顺序

参考 `folio-2025` 的 `Time.js` 和其 [游戏循环说明](https://github.com/brunosimon/folio-2025#game-loop)，学习将输入、角色、镜头、环境与渲染组织成有明确先后的阶段。本项目据此关注动作与阅读的顺序：准备 → 角色投骰 → 点数确定 → 沿路线行走 → 抵达转场 → 中央事件问答。

这是本项目自己的动作流程；没有移植对方的物理车辆系统。到站以前不会提前让问题覆盖行走，查看规则或来源时也不会开始下一步。默认自动出发使玩家主要关注生活选择，关闭后可在准备状态手动继续。AI 台词异步出现，不阻塞继续按钮。

## 六面骰子的表现

查看了 [dice-box-threejs](https://github.com/3d-dice/dice-box-threejs) 和 [threejs-dice](https://github.com/byWulf/threejs-dice)，借鉴投掷动作、最终骰面和规则点数需要保持一致的设计思路；两者所见许可证为 MIT。

本项目用均匀六面随机数先确定结果，再由 `src/dice.js` 展示角色投掷、弹跳和最后朝向。所有点数仍有相同概率。当前没有引入上述库或真实刚体引擎，视觉弹跳不能解释为物理仿真的结果。验收关注六种点数的显示、角色动作的衔接以及落地后再移动，而不是宣称具有现实骰子的物理精度。

## 连续路线与结束条件

旧版每季小环路改为一条连续道路。完整路线 24 格，快速路线 12 格，顺序由 `src/route.js` 统一定义。骰子每次向前，经过的格子不触发；越过终点时停在终点，完成最后一个选择后结算。只要资源归零就先结束，不能依靠到达终点绕过耗尽条件。

均匀六面骰的理论平均行进次数约为 7.33 和 3.91，阅读时间会影响总时长。因此界面只给出体验目标，不再承诺固定八回合或四回合。存档升级为 v2，旧记录保留在原存储键，不强行换算到新路线。

## 当前可验证与待体验部分

引擎测试可验证路线、结算、结束优先级与存档重放；模型测试使用模拟执行器验证缓存、队列、超时和真实账本约束。场景与界面使用 `tests/experience-browser.mjs`，骰子使用 `tests/dice-browser.mjs` 检查，截图产物放在 `test-results/`。

连续空间、植物层次和中央问答是本版已实现的改动。镜头舒适度、阅读节奏、模型台词质量与真实试玩难度仍需要体验反馈。角色继续沿用根据刘看山素材自行搭建的几何模型，精细骨骼动作、驾驶或自由探索不在当前实现中。
