# 给朋友试玩

本功能把一份独立构建的游戏临时分享出去，不会发布开发用的 4173 服务，不需要上传 GitHub。

## 本次验收记录（2026-09-08）

口令网关与本地生产试玩已通过验证：23 个模型正常加载，题目与选项正常，未调用官方模型。自动测试共 64 项，其中 63 项通过、1 项可选浏览器测试跳过；独立生产浏览器检查另行通过。

公网尚未验收通过：多次临时域名解析或 HTTPS 连接失败，最后一次真实 Chrome 报 `ERR_CONNECTION_CLOSED`。失败的分享进程已关闭，未交付可用公网链接，本机 4173 游戏保持正常。可更换网络或现有代理节点后再次启动测试；不需要把知乎密码或 API 密钥提供给朋友。下方操作说明描述已实现的功能，不代表链接目前正在运行。

## 开始与停止

双击 `启动试玩分享.cmd`。它会生成本次的独立口令、保存一份游戏构建，并依次启动生产服务、口令网关和 Cloudflare 临时转发。成功后显示分享网址与口令，同时保存在 `.private-share/share-info.txt`。把网址和口令私下发给受邀朋友即可；不要发自己的知乎密码或 API 密钥。

双击 `停止试玩分享.cmd` 可以关闭这次分享。脚本按进程 ID、创建时间、程序路径和启动参数核对归属，只关闭本功能启动的三个进程，保留本地开发游戏。不会删除源代码、存档或私人配置。

再次启动会生成新的网址与口令。已有分享运行时重复启动会提示复用，不会产生多份后台服务。用 `powershell -NoProfile -ExecutionPolicy Bypass -File .\share-playtest.ps1 -Action Status` 可以查看这三个进程的状态。

## 需要知道

- 电脑需要保持开机、联网，且不能进入睡眠；启动窗口关闭不影响后台分享服务。
- 这是临时试玩通道，不是长期托管。不同地区、运营商可能无法连接或速度较慢，需要实际测试。
- 本机公网检查沿用电脑现有的网络代理设置，不会更改系统代理、DNS 或防火墙。本机验证成功不等于朋友所在网络一定能直连；请先邀请一位朋友实测。3D 素材首次加载可能需要等待。
- 朋友不必安装 Node.js，也不需要自己的知乎账号；游戏进度保存在各自浏览器。公网域名改变后浏览器不会自动把旧域名存档迁移过去。
- 知乎和 AI 请求使用分享者已配置的官方额度。网关有登录、请求频率与并发限制；模型后端另有调用上限。受到限制时可继续体验预设事件和备用回顾，不保证多人同时实时生成。
- 请求限制是本机进程内的保护，不是平台官方额度；重启会重置计数。不要将口令发布到公开群组或论坛。
- `.private-share/` 只允许当前 Windows 用户和 SYSTEM 访问，且已加入 Git 忽略。它含访问口令与运行日志，不应手动上传或转发整个目录。
- 浏览器必须下载前端游戏代码和美术资源才能运行，因此受邀玩家可以查看这部分内容；服务端密钥、CLI 凭据与项目源目录不会通过网关发布。

## 技术边界

Cloudflare 临时 HTTPS 地址 → 127.0.0.1:4175 口令网关 → 127.0.0.1:4174 生产游戏。转发工具的就绪检查仅绑定本机 20491；原开发服务继续使用 4173。

网关使用不透明会话 Cookie、HttpOnly、Secure、SameSite=Strict、同源 POST 校验、静态路径白名单、请求大小与速率限制，不代理 WebSocket，也不向游戏进程转发 Cookie、Authorization 或客户端转发头。会话最长 12 小时，网关重启即失效。用户点击外部分享链接进入登录页是允许的，跨站 API 请求和跨站提交仍然拒绝。

Cloudflared 仅使用[官方发布](https://github.com/cloudflare/cloudflared/releases/tag/2026.8.3)的 Windows 程序，启动前校验 SHA-256。版本、下载地址和摘要记录在 `tools/cloudflared-source.json`。[Quick Tunnel 官方说明](https://developers.cloudflare.com/cloudflare-one/networks/connectors/cloudflare-tunnel/do-more-with-tunnels/trycloudflare/)明确其仅适合测试，不提供可用性保证。

## 验证

`npm test` 包含口令页与网关的隔离测试，不使用官方额度。`node tests/share-login-browser.mjs` 检查独立口令页布局。设置 `RUN_PUBLIC_SHARE_CHECK=1` 后运行 `node tests/share-public-browser.mjs` 才会检查当前正在运行的真实 HTTPS 通道：脚本从私有配置读取口令，不把口令写入截图、URL或命令行；有效叙事请求在浏览器中被模拟，不消耗模型额度。

`node tests/share-production-browser.mjs` 可以隔离验证本机生产构建、口令网关、全部模型与题目流程；它不表示公网已经连通。
